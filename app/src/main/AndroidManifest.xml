<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />

    <permission
        android:name="de.fgna.androidllmservice.permission.BIND_LLM_SERVICE"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="false"
        android:label="Android LLM Service"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidLlmService">

        <uses-native-library android:name="libvndksupport.so" android:required="false" />
        <uses-native-library android:name="libOpenCL.so" android:required="false" />

        <service
            android:name=".LlmBinderService"
            android:exported="true"
            android:permission="de.fgna.androidllmservice.permission.BIND_LLM_SERVICE">
            <intent-filter>
                <action android:name="de.fgna.androidllmservice.BIND" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
